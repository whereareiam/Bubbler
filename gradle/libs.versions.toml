[versions]

# plugins
shadow = "9.4.2"
toolkit = "dev-8c0271b"

socialismus = "dev-217410a"

# compile time processing
lombok = "1.18.46"

# general
guice = "7.0.0"
cloud = "2.0.0"
annotations = "26.1.0"
adventure = "4.25.0"
packetevents = "2.12.2"
commandant = "dev-42806f6"
configura = "dev-537309f"

# testing
junit = "6.1.0"
mockito = "5.23.0"

[libraries]

# plugins
shadow-gradle-plugin = { group = "com.gradleup.shadow", name = "shadow-gradle-plugin", version.ref = "shadow" }
toolkit-publish-maven = { group = "me.whereareiam.toolkit", name = "publish-maven", version.ref = "toolkit" }

socialismus-module-api = { group = "me.whereareiam.socialismus.module", name = "api", version.ref = "socialismus" }

# compile time processing
lombok = { group = "org.projectlombok", name = "lombok", version.ref = "lombok" }

# general
guice = { group = "com.google.inject", name = "guice", version.ref = "guice" }
cloud-annotations = { group = "org.incendo", name = "cloud-annotations", version.ref = "cloud" }
annotations = { group = "org.jetbrains", name = "annotations", version.ref = "annotations" }
adventure = { group = "net.kyori", name = "adventure-api", version.ref = "adventure" }
packetevents = { group = "com.github.retrooper", name = "packetevents-spigot", version.ref = "packetevents" }
commandant = { group = "me.whereareiam", name = "commandant", version.ref = "commandant" }
configura = { group = "me.whereareiam", name = "configura", version.ref = "configura" }
configura-feature-polymorphic = { group = "me.whereareiam.configura.feature", name = "polymorphic", version.ref = "configura" }

# testing
junit-jupiter = { group = "org.junit.jupiter", name = "junit-jupiter", version.ref = "junit" }
junit-platform = { group = "org.junit.platform", name = "junit-platform-launcher", version.ref = "junit" }
mockito-core = { group = "org.mockito", name = "mockito-core", version.ref = "mockito" }
mockito-junit = { group = "org.mockito", name = "mockito-junit-jupiter", version.ref = "mockito" }

[bundles]

testing = ["junit-jupiter", "mockito-core", "mockito-junit"]

[plugins]

shadow = { id = "com.gradleup.shadow", version.ref = "shadow" }
