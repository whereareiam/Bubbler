[versions]

# plugins
shadow = "8.3.6"

socialismus = "dev"

# compile time processing
lombok = "1.18.36"

# general
guice = "7.0.0"
adventure = "4.19.0"
jackson = "2.17.2"
packetevents = "2.7.0"

# cloud
cloud-core = "2.0.0"

[libraries]

socialismus = { group = "me.whereareiam", name = "Socialismus", version.ref = "socialismus" }

# compile time processing
lombok = { group = "org.projectlombok", name = "lombok", version.ref = "lombok" }

# general
guice = { group = "com.google.inject", name = "guice", version.ref = "guice" }
adventure = { group = "net.kyori", name = "adventure-api", version.ref = "adventure" }
jackson = { group = "com.fasterxml.jackson.core", name = "jackson-databind", version.ref = "jackson" }
packetevents = { group = "com.github.retrooper", name = "packetevents-spigot", version.ref = "packetevents" }

# cloud
cloud-core = { group = "org.incendo", name = "cloud-core", version.ref = "cloud-core" }
cloud-annotations = { group = "org.incendo", name = "cloud-annotations", version.ref = "cloud-core" }

[bundles]

cloud = ["cloud-core", "cloud-annotations"]

[plugins]

shadow = { id = "com.gradleup.shadow", version.ref = "shadow" }